# Primary RVC WebUI Drop-In

Place a complete RVC WebUI deployment here.

Expected markers:

- `infer-web.py`
- `assets/weights/`
- `assets/indices/`
- `logs/`

The launcher expects this primary instance on port `7866`.
Training remains primary-only unless a later ADR approves backup training.
